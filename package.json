{
    "name": "aws-saml-cli",
    "version": "1.0.0",
    "description": "A CLI tool to fetch AWS credentials via SAML authentication",
    "bin": {
        "aws-saml": "bin/aws-saml"
    },
    "scripts": {
        "build": "tsc",
        "start": "node dist/cli.js"
    },
    "devDependencies": {
        "@types/node": "^24.0.14",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "commander": "^14.0.0",
        "inquirer": "^12.7.0"
    }
}
